version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    command: ["npm", "run", "test"]
  test:
    build:
      context: .
      dockerfile: Dockerfile.production
    command: ["npm", "test"]