version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    command: ["npm", "test"]
  test:
    build:
      context: .
      dockerfile: Dockerfile.production